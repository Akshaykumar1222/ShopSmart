// server/routes/placeholder.js

const express = require("express");
const router = express.Router();

// Sample route
router.get("/test", (req, res) => {
  res.json({ message: "This is a placeholder route!" });
});

module.exports = router;
